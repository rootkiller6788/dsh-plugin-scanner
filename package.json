{
  "name": "dsh-plugin-scanner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Security scanner for DeepSeek Harness (dsh) plugins, built on the dsh plugin kernel",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
